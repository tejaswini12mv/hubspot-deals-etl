# HubSpot Deals ETL

This project is an ETL service for HubSpot Deals data.

## Setup
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Add `.env` file with your HubSpot token

## Usage
- Run ETL: `python app.py`

